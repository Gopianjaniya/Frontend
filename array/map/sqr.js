let arr = [2, 3, 4, 6, 7, 9, 5, 33]
    //<------------- return new array but no return value ----->
let newArr = arr.map((val) => {
    return val * val
        //  gien a square 
})
console.log(newArr);